import { LoginForm } from "@/components/auth/LoginForm";

/**
 * 로그인 페이지
 * @description 사용자 로그인을 위한 페이지입니다. 루트 페이지와 동일한 컴포넌트를 사용합니다.
 */
export default function LoginPage() {
  return <LoginForm />;
}

